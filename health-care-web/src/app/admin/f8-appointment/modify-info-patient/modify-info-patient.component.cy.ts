import { ModifyInfoPatientComponent } from './modify-info-patient.component';

describe('ModifyInfoPatientComponent', () => {
  it('should mount', () => {
    cy.mount(ModifyInfoPatientComponent);
  });
});
