import { ResetPasswordUserComponent } from './reset-password-user.component'

describe('ResetPasswordUserComponent', () => {
  it('should mount', () => {
    cy.mount(ResetPasswordUserComponent)
  })
})